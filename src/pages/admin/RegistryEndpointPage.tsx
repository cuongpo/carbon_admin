import React from 'react';
import Layout from '../../components/layout/Layout';
import RegistryEndpoint from '../../components/admin/RegistryEndpoint';

const RegistryEndpointPage: React.FC = () => {
  return (
    <Layout>
      <RegistryEndpoint />
    </Layout>
  );
};

export default RegistryEndpointPage;
