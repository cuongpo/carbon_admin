import React from 'react';
import Layout from '../../components/layout/Layout';
import MintHistory from '../../components/minter/MintHistory';

const MintHistoryPage: React.FC = () => {
  return (
    <Layout>
      <MintHistory />
    </Layout>
  );
};

export default MintHistoryPage;
