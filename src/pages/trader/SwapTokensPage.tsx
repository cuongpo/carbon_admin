import React from 'react';
import Layout from '../../components/layout/Layout';
import SwapTokens from '../../components/trader/SwapTokens';

const SwapTokensPage: React.FC = () => {
  return (
    <Layout>
      <SwapTokens />
    </Layout>
  );
};

export default SwapTokensPage;
